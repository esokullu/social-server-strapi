{
    "routes": [
      {
        "method": "GET",
        "path": "/v1/whoami",
        "handler": "authentication.whoami",
        "config": {
          "policies": []
        }
      },
      {
        "method": "GET",
        "path": "/v1/login",
        "handler": "authentication.login",
        "config": {
          "policies": []
        }
      }
    ]
}